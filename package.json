{
  "name": "spacecat-halving-tracker",
  "version": "1.0.0",
  "description": "Bitcoin Halving Tracker — SpaceCat Edition",
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-i18n-pages.js && node scripts/sync-about-pages.js",
    "dev": "vite",
    "prebuild": "node scripts/sync-i18n-pages.js && node scripts/sync-about-pages.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "vite": "^5.4.0"
  }
}
